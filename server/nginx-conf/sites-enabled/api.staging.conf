server {
    listen    443 ssl;
		listen 		[::]:443;
		add_header api staging;
		server_name		beta.staging.api.open.anton.pizza;
		add_header X-Content-Type-Options nosniff;
		add_header X-Frame-Options SAMEORIGIN;
		location / {
			proxy_pass http://localhost:8090;
		}
}