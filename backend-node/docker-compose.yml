version: '3'
services:
  postgres:
    image: postgres:10.5
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: make_chabo_open_again
    volumes:
      - ./db/.pgdata:/var/lib/postgresql/data
